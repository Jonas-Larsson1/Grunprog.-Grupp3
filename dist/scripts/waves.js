let wavesButton = document.getElementById("wavesButton")
let wavesContainer = document.getElementById("wavesContainer")

wavesButton.addEventListener("click", () => {
    wavesText()
})

const wavesText = () => {
    

}
 




/*
export const wavesMessage = (game) => {
    if(game.timer < 30) {
        
    } else if (game.timer > 30 && game.timer < 60) {
        //2nd wave
    } else if (game.timer > 60) {
        //Final wave
    }
}
*/